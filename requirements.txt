# requirements.txt - Updated for Real-time Audio Classification

# Core MemryX SDK
memryx

# AI Edge LiteRT for TensorFlow Lite model inference
ai-edge-litert==1.3.0

# Scientific computing library for audio processing
scipy

# Web framework for the Flask application
flask==3.1.1

# Real-time WebSocket communication
flask-socketio==5.3.5

# ffmpeg for audio processing
ffmpeg-python

# Additional dependencies for real-time processing
numpy>=1.21.0

# Note: ffmpeg needs to be installed separately via system package manager
# For Linux: sudo apt install ffmpeg
# For macOS: brew install ffmpeg
# Alternative: pip install ffmpeg-python (if system ffmpeg installation fails)